import 'package:flutter/material.dart';

class Favoriate extends StatelessWidget {
  const Favoriate({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
        alignment: Alignment.center, child: const Text('Favoriate'));
  }
}
